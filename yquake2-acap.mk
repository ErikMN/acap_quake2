# Yamagi Quake II configuration for ACAP

YQ2_OSTYPE := Linux
YQ2_ARCH := aarch64

WITH_CURL := no
WITH_EXECINFO := no
WITH_OPENAL := no
WITH_RPATH := no
WITH_SYSTEMWIDE := no
WITH_XDG := no
WITH_SDL3 := no

# Let the ACAP SDK toolchain provide the system include paths.
INCLUDE :=
